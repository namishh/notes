import 'tailwindcss/tailwind.css'
import { GeistSans } from "geist/font/sans";
import '../styles/globals.css'

export default function App({ Component, pageProps }) { return <Component className={GeistSans.className} {...pageProps} /> }

